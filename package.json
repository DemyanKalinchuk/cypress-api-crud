{
  "name": "cypress-api-crud",
  "version": "1.0.0",
  "private": true,
  "description": "Cypress API testing framework derived from Playwright-api-javascript-crud. Covers CRUD flows against reqres.in with logging, schema validation, and Mochawesome reports.",
  "scripts": {
    "cypress:open": "cypress open",
    "test": "node ./scripts/pretest.js && cypress run",
    "test:headed": "node ./scripts/pretest.js && cypress run --headed",
    "report": "node ./scripts/generate-report.js",
    "ci": "node ./scripts/pretest.js && cypress run --reporter cypress-mochawesome-reporter && npm run report"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "cypress": "^13.17.0",
    "cypress-mochawesome-reporter": "^3.8.2",
    "dotenv": "^16.4.5",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
